/**
 * Testlash (Testing):
 * Bu fayl unit testlar qanday yozilishini ko'rsatadi.
 * Asosan Vitest va React Testing Library ishlatiladi.
 */

/* 
// Misol (Sintaksis):
import { render, screen } from '@testing-library/react';
import { expect, test } from 'vitest';
import UseToggle from '../hooks/useToggle/useToggle';

test('useToggle hooki qiymatni o'zgartirishi kerak', () => {
  // Test mantiqi bu yerda yoziladi
});
*/

console.log("Tests papkasi: Bu yerda unit va integration testlar saqlanadi.");
